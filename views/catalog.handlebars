<div class="container">
    <div class="row">
        {{#each products}}
        <div class="col-md-6 col-lg-3">
            <div class="productimg mt-5 mb-2"></div>
            <h2>{{product}}</h2>
            <h4>{{desc}}</h4>
        </div>
        {{/each}}
    </div>
</div>